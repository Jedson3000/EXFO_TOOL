{
    "name": "exfo-test-setup",
    "version": "1.0.0",
    "description": "EXFO Test Setup Tool",
    "main": "main.js",
    "scripts": {
        "start": "electron .",
        "package-win": "electron-packager . exfo-test-setup --platform=win32 --arch=x64 --icon=assets/icon.ico --out=dist --asar --overwrite"
    },
    "dependencies": {
        "electron": "^13.6.9"
    },
    "devDependencies": {
        "electron-packager": "^15.5.2"
    }
}
